{
  "name": "@wasm-forge/radix_sort",
  "version": "0.1.0",
  "private": true,
  "main": "radix_sort.js",
  "types": "radix_sort.d.ts",
  "scripts": {
    "build:nodejs": "cargo build --release --target wasm32-unknown-unknown && wasm-bindgen --target nodejs --out-dir pkg-test ../../target/wasm32-unknown-unknown/release/radix_sort.wasm"
  }
}
