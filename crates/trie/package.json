{
  "name": "@wasm-forge/trie",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./pkg/trie.js",
  "types": "./pkg/trie.d.ts",
  "files": [
    "pkg"
  ],
  "scripts": {
    "build": "cargo build --release --target wasm32-unknown-unknown && wasm-bindgen --target web --out-dir pkg ../../target/wasm32-unknown-unknown/release/trie.wasm",
    "build:nodejs": "cargo build --release --target wasm32-unknown-unknown && wasm-bindgen --target nodejs --out-dir pkg-test ../../target/wasm32-unknown-unknown/release/trie.wasm"
  },
  "devDependencies": {}
}
